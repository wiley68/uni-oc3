{
  "contract_ids": ["DEPLOY-001", "DEPLOY-002", "DEPLOY-003"],
  "decision": "D3",
  "status": "recommendation_pending_approval",
  "source": "reference-uni-oc4 (parity with completed UniCredit Woo/PS8/PS9/OC4 convention)",
  "filenames": {
    "passphrase": "secrets/smartucf-key.php",
    "passphrase_array_key": "passphrase",
    "certificate": "keys/avalon_cert.pem",
    "private_key": "keys/avalon_private_key.pem",
    "cp_environment": "config/environment.php",
    "cp_environment_key": "control_panel_url"
  },
  "permissions": {
    "certificate": "0640",
    "private_key": "0600",
    "passphrase_file": "0600",
    "temp_consumer_lease": "0600"
  },
  "key_sources": {
    "smartucf_passphrase": "local secrets/smartucf-key.php only; never CP, never settings, never env, never request",
    "cp_login_secret": "admin setting encrypted at rest; operator-entered; never re-displayed",
    "settings_encryption_key": "HKDF-SHA256 from OpenCart DB_PASSWORD (config.php); 32 bytes; info mt_uni_credit/settings-encryption/v1",
    "no_predictable_fallback": true
  },
  "health_checks": [
    "presence of passphrase file / PEM pair",
    "readable by PHP handler user",
    "mode/owner/group",
    "PEM SHA-256 of exact bytes",
    "certificate/key match and dates",
    "never echo secret contents"
  ],
  "oc3_path_translation": {
    "oc4_root": "extension/mt_uni_credit/{secrets,keys,config}",
    "oc3_ocmod_extracts_to": "shop document root via upload/",
    "recommendation": "Keep the same relative filenames. Resolve the directory from a module root helper. Prefer a protected location (DIR_STORAGE/mt_uni_credit or equivalent) if the shop document root would expose PEM/passphrase; document any difference as an OC3 filesystem constraint, not a new secret format.",
    "pending": "Developer confirms whether the OC3 test filesystem can host secrets/ and keys/ beside the shop root or must use DIR_STORAGE."
  }
}
